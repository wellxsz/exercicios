let mes = 'dezembro'
if ((mes === 'dezembro') || (mes === 'Dezembro') || (mes === 'janeiro') || (mes === 'Janeiro') || (mes === 'julho') || (mes === 'Julho') ) {
    console.log ('Férias!')
} else {
    console.log ('Vai estudar')
}