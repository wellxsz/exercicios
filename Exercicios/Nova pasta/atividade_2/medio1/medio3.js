const phrase = 'Wellington'
let charac = phrase.length

if (charac > 5) {
    console.log ('Seu texto tem mais de 5 caracteres, ele tem', charac, 'caracteres.')
} else {
    console.log ('Seu texto tem menos de 5 caracteres')
}
